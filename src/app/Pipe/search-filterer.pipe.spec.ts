import { SearchFiltererPipe } from './search-filterer.pipe';

describe('SearchFiltererPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFiltererPipe();
    expect(pipe).toBeTruthy();
  });
});
